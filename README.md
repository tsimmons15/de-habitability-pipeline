#Data Engineering Habitability Pipeline Readme

Python environment:
Python 3.11.11

It is recommended to use a virtual environment. The provided requirements.txt contains the versions for the various modules used.

Setting up the environment using venv:
 `python -m venv .venv`
 `source .venv/bin/activate`
 `pip install -r requirements.txt`


